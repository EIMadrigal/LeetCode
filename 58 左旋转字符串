class Solution {
public:
    string reverseLeftWords(string s, int n) {
        if (s == "") {
            return "";
        }
        n = n % s.length();
        string sub = s.substr(0, n);
        s.erase(0, n);
        return s + sub;
    }
};
